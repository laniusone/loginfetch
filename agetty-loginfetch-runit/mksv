#!/bin/sh -e
tty=tty${1}
if [ -d agetty-${tty} ];then
  echo "This tty already exists"
  exit 1
fi
mkdir agetty-${tty}
ln -s /etc/sv/agetty-loginfetch/run agetty-${tty}/
ln -s /etc/sv/agetty-loginfetch/finish agetty-${tty}/
